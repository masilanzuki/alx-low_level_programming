#include "main.h"
/**
 * _abs - prints the absolute value of a number
 * @r: r is an integer
 *
 * Return: integer
 */
int _abs(int r)
{
	if (r > 0)
	{
		return (r);
	}
	else if (r < 0)
	{
		return (-r);
	}
	else
	{
		return (r);
	}
}
