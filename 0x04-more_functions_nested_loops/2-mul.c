#include "main.h"

/**
 * mul - multiplies two intergers .
 *
 * @a: the first number.
 * @b: the second number.
 *
 *Return: the multiplacation of two numbers.
 */
int mul(int a, int b)
{
return (a * b);
}
