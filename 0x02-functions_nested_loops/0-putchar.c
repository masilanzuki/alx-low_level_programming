#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char T[8] = "_putchar";
int i = 0;

for (i = 0; i < 8; i++)
{
putchar(T[i]);
}
putchar('\n');
return (0);
}
